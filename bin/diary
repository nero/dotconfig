#!/bin/sh
cfg=~/.config/diaryrc

remote=""
stamp=$(date +%Y-%m-%d)

test -f "$cfg" && . "$cfg"

cmd="cd \$HOME; mkdir -p diary; (echo; date -R) >>diary/$stamp; nano +-1 diary/$stamp"

if test -n "$remote"; then
  ssh -t "$remote" "$cmd"
else
  sh -c "$cmd"
fi
