if test -z "$DISPLAY" && \
    expr "$(tty)" : '/dev/tty[0-9][0-9]*$' > /dev/null && \
    command -v startx >/dev/null 2>&1 && \
    command -v i3 >/dev/null 2>&1; then

  XAUTHORITY=${TMPDIR:=/tmp}/${LOGNAME:?}.Xauthority \
    exec startx "${HOME:?}"/.config/xinitrc
fi
