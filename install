#!/bin/sh
ln -sfn "$HOME"/.config/profile "${HOME}"/.profile

case "$SHELL" in
(*/bash)
  rm "$HOME/.bash_profile"
  ln -sfn "$ENV" "$HOME/.bashrc"
  ;;
(*/zsh)
  ln -sfn "$ENV" "$HOME/.zshrc"
  ;;
esac

for f in "$HOME"/.config/*/install; do
  c=${f%/*}
  command -v "${c##*/}" >/dev/null 2>&1 && . "$f"
done
